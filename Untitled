ALTER TABLE recordings 
ADD COLUMN IF NOT EXISTS storage_type text,
ADD COLUMN IF NOT EXISTS storage_bytes bigint;
```

---

**Step 2 — Add Cloudinary env vars in Vercel**

Go to Vercel → Your Project → Settings → Environment Variables → add:
```
CLOUDINARY_CLOUD_NAME = dw15kdopv
CLOUDINARY_API_KEY = (from Cloudinary API Keys page)
CLOUDINARY_API_SECRET = (from Cloudinary API Keys page)
CLOUDINARY_UPLOAD_PRESET = Transcribe AI voicevault